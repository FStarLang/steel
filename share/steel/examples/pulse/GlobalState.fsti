module GlobalState

//
// A quick implementation of global state in Pulse
// See GlobalState.fst for the actual code
// This interface file is there since we briefly break the stt abstraction with friends
//   and F* requires us to have an interface when we do so
//
